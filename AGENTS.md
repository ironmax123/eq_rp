"このプロジェクトのFastAPIはローカル起動のみを想定しいます"

## 作業ルール

- プランモードで提示するプランも日本語でやること。
- 機能実装は基本的にコードを書く前に `docs/plan/<機能名>_yyyymmdd.md` を作成すること。その時、機能の具体的な実装計画書として作成すること。
- bugfix のタスクは基本的にバグを修正する前に、考えられる原因と対策、修正計画を `docs/fix-plan/<バグ内容>_yyyymmdd.md` にまとめること。
- 実装計画書および修正計画書を記述してからコード変更すること。コード変更は許可が出た場合のみ行い、勝手な変更は避けること。

## 確認用スクリプトの扱い

- Thonny や CLI で確認するための単体スクリプトを作る場合は、「何と同じ挙動を確認するためのファイルか」を最初に固定すること。
- `main.py` の挙動確認が目的なら、確認用スクリプトの音列・定数・タイミングは `main.py` と一致させること。
- 参考コードがある場合でも、勝手に短縮・拡張・別パターン化しないこと。変更が必要なら、変更意図を明示して許可を得ること。
- 「NHK 風」など特定の目標音・目標挙動が指定されている場合、参考コードは実装方法の参考として扱い、参考コードの音列や挙動をそのまま採用する意味に読み替えないこと。

## 主観要素の実装

- 音、UI、文言、タイミングなど主観が入る要素は、実装者判断で「良さそうな形」に変えないこと。
- ユーザーが「同じ」「参考に」「確認したい」と言っている場合は、何と一致させるのかを優先して扱うこと。

## 反省と再発防止

- 対応ミスを反省する場合は、会話に出すだけで終わらせないこと。
- 必要に応じて `docs/reflection/<内容>_yyyymmdd.md` に反省内容、原因、再発防止策を残すこと。
- 再発防止として今後の作業ルールに関わる内容は、この `AGENTS.md` にも追記すること。
